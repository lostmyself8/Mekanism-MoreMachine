spotless {
    encoding 'UTF-8'

    format 'misc', {
        target '.gitignore'

        trimTrailingWhitespace()
        leadingTabsToSpaces()
        endWithNewline()
    }
    java {
        target 'src/main/java/**/*.java', 'src/test/java/**/*.java'

        def orderFile = file("$rootDir/gradle/spotless/spotless.importorder")
        def formatFile = file("$rootDir/gradle/spotless/spotless.eclipseformat.xml")
        toggleOffOn()
        importOrderFile(orderFile)
        // The JavaParser-based unused import step rejects Java 25 unnamed lambda parameters (_).
        endWithNewline()
        eclipse('4.36').configFile(formatFile)
    }
    json {
        target('src/*/resources/**/*.json', 'src/*/resources/**/*.mcmeta')
        targetExclude 'src/generated/resources/**'
        biome()
        trimTrailingWhitespace()
        leadingSpacesToTabs()
        endWithNewline()
    }
}
